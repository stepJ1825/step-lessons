#spring.profiles.active: test
spring:
  datasource:
    url: jdbc:h2:mem:test_mem;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username:
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never
#      mode: embedded  # выполняется только для встроенных БД
#      mode: always
#      schema-locations: classpath:sql/V3.0.1__Create_tables.sql
#      data-locations:
#        - classpath:sql/V3.0.2__Insert_authors.sql
#        - classpath:sql/V3.0.3__Insert_genres.sql
#        - classpath:sql/V3.0.4__Insert_books.sql
  flyway:
    enabled: false
  jpa:
    show_sql: true
    format_sql: true
